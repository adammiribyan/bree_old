<% title "Редактирование шаблона" %>

<h1 class="special">Редактирование шаблона</h1>

<div class="liner">
  <%= render "form" %>
</div>

<% content_for(:sidebar) { %>  
  <%= render "sidebar" %>
<% } %>
