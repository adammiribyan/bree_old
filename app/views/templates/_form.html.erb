<%= simple_form_for(@template) do |f| %>
  <%= f.error_notification %>

  <div class="inputs">
    <%= f.input :name %>
    <%= f.input :body %>
    <%= f.input :doctor_id %>
  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
