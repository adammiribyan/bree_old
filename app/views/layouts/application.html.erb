<!DOCTYPE html>
<html lang="en-US">
  <head>
    <meta content="text/html; charset=utf-8" http-equiv="Content-Type">
    <title><%= yield :title %></title>
    <%= stylesheet_link_tag "application", "jquery-ui.custom" %>
    <%= javascript_include_tag "jquery.min", "jquery.pjax", "jquery-ui.custom.min", "autoresize.jquery.min", "jquery.jeditable.min", "application", "rails", "autocomplete-rails.js" %>     
    <%= csrf_meta_tag %>
  </head>
  <body class="<%= body_classes.join(" ") if body_classes.present? %>">    
    <%= render "layouts/header" %>
    <div id="layout"> 
      <div class="inner">
        <div id="content" class="clearfix">
          <%= yield %>
        </div>
        <% unless @without_sidebar %>
          <div id="sidebar" class="clearfix">
            <%= yield :sidebar %>          
          </div>        
        <% end %>
        <div id="footer" class="clearfix">
          <p>Managed by <a href="<%= AppConfig[:app_link] %>"><%= AppConfig[:app_name] %></a>.</p>
        </div>        
      </div>      
    </div>
    <div id="loading"><%= image_tag "dots-white.gif" %></div>
  </body>  
</html>