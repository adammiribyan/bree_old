<%= simple_form_for(@appointment) do |f| %>
  <%= f.error_notification %>

  <div class="inputs">
    <%= f.association :patient %>
    <%= f.association :doctor %>
  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
